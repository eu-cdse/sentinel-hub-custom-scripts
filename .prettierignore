# Hugo build output and caches
public
resources
.hugo_build.lock

# Dependencies and generated files
node_modules
package-lock.json

# Hugo templates. Prettier's built-in html parser mangles Go template
# syntax ({{ ... }}), so layouts/ are excluded for now. When
# prettier-plugin-go-template is added, delete the line below
# and this comment.
layouts
